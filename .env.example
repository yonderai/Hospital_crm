# Database Connection - Add your MongoDB URI here
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/hospital_crm?retryWrites=true&w=majority"

# Authentication
NEXTAUTH_SECRET="your-super-secret-key-change-this"
NEXTAUTH_URL="http://localhost:3000"

# Optional Backend Config (if running separate backend)
PORT=5001
NODE_ENV=development
